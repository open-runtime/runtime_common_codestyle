name: runtime_common_codestyle
description: Common code style and analysis rules for OpenRuntime monorepo - enforces trailing commas, strong typing, and custom naming conventions
version: 0.1.6
homepage: https://github.com/open-runtime/aot_monorepo
repository: https://github.com/open-runtime/aot_monorepo
environment:
  sdk: ^3.9.0
dependencies:
  # ==========================================================================
  #
  # Direct pub.dev dependencies
  #
  # ==========================================================================
  analyzer: ^8.0.0
  lint_hard: ^6.0.0

dev_dependencies:
  custom_lint: ^0.8.1
  custom_lint_builder: ^0.8.1
